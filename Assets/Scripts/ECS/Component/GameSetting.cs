using UnityEngine;

public static class GameSetting
{
    // Game tile width
    public static readonly int MAP_WIDTH = 50;

    // Game tile height
    public static readonly int MAP_HEIGHT = 50;

    // Width per grid
    public static readonly float GRID_WIDTH = 1;

    // Height per grid
    public static readonly float GRID_HEIGHT = 1;
}